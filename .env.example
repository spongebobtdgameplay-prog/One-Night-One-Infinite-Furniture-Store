# Never put real credentials in this file.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE
NODE_ENV=production
# Optional: comma-separated additional browser origins.
ALLOWED_ORIGINS=https://spongebobtdgameplay-prog.github.io
# Optional: set true only if your Postgres URL requires TLS but does not include sslmode=require.
PGSSL=false
